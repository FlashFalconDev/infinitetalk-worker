# ===== InfiniteTalk Worker Docker 配置 =====

# API Base URL
INFINITETALK_API_BASE=https://host.flashfalcon.info

# Worker Token（從 Admin 後台獲取）
INFINITETALK_WORKER_TOKEN=your_token_from_admin_here

# Worker 名稱（可選）
WORKER_NAME=docker-worker-01

# Worker 地區（可選）
WORKER_REGION=us-west-1

# ===== 多 Worker 配置（如果有多張 GPU）=====

# Worker 2 Token
INFINITETALK_WORKER_TOKEN_2=your_second_token_here

# Worker 2 名稱
WORKER_NAME_2=docker-worker-02
